pipeline {
    agent any {
        stages {
            stage('dependencies') {
                steps {
                    echo 'installing the dep'
                }
            }
            // stage('run') {
            //     steps {

            //     }
            // }
            // stage('test') {
            //     steps {

            //     }
            // }
        }
    }
}