pipeline {
    agent any {
        environment {
            VERSION = '1.0.0'
        }
        stages {
            stage('dependencies') {
                steps {
                    echo 'installing the dep'
                }
                steps {
                    echo "this is version ${VERSION}"
                }
            }
            // stage('run') {
            //     steps {

            //     }
            // }
            // stage('test') {
            //     steps {

            //     }
            // }
        }
    }
}